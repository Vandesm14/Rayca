{
  "type": "module",
  "scripts": {
    "fmt": "prettier --write ./src",
    "build": "parcel build ./src/index.html --dist-dir ./docs",
    "build:watch": "parcel ./src/index.html --dist-dir ./docs",
    "clean": "rm -rf ./docs"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.2",
    "parcel": "^2.3.2",
    "parcel-reporter-static-files-copy": "^1.3.4",
    "prettier": "^2.5.1"
  }
}
